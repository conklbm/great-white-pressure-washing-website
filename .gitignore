# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# personal Claude Code settings (not shared)
.claude/settings.local.json

# Internal working brief — this repo is PUBLIC and this is a real, named
# family business, so the rank-and-rent framing stays out of it. Remove this
# line if you decide you want it published.
rank-and-rent-strategy.md

# local screenshot/debug artifacts
.playwright-mcp/

# Raw camera drops. Source HEICs aren't web-servable and shouldn't ship —
# the converted, paired JPEGs in public/gallery/ are what the site uses.
# Keep originals here locally; they just don't belong in the repo.
public/dylan - before and after photos/
